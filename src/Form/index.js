import "./style.css";

const Form = () => (
    <form className="form">
        <input className="form__field" placeholder="Enter a task" />
        <button className="form__button">Add Task</button>
    </form>
)

export default Form;